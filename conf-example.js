// Copy this file content into a file name conf.js and adjust the values

const conf = {
    BASE_URL: 'http://localhost:5000/api',
    USERNAME: 'username',
    PASSWORD: 'password'
};

export default conf